{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2018",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "jsx": "react",                                      /* Specify what JSX code is generated. "react" is suitable for React JSX. */

    /* Modules */
    "module": "nodenext",                                /* Specify what module code is generated. 'nodenext' for modern Node.js. */
    "rootDir": "./src",                                  /* Specify the root folder for your tool's source files. */
    "moduleResolution": "nodenext",                      /* Specify how TypeScript looks up a file from a given module specifier. */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. */

    /* Emit */
    "outDir": "./dist",                                  /* Specify an output folder for all emitted JavaScript files. */
    "declaration": true,                                 /* Generate .d.ts files for your compiled code. */
    "sourceMap": true,                                   /* Create sourcemaps for debugging. */
    "removeComments": true,                              /* Remove comments from compiled JavaScript. */

    /* Type Checking */
    "strict": true,                                      /* Enable all strict type-checking options for robust code. */
    "forceConsistentCasingInFileNames": true,            /* Ensure that casing is correct in imports. */
    "noImplicitAny": true,                               /* Warn if any variable has an implicit 'any' type. */
    "noUnusedLocals": true,                              /* Report errors on unused local variables. */
    "noUnusedParameters": true,                          /* Report errors on unused function parameters. */
    "noImplicitReturns": true,                           /* Report errors on code paths that don't explicitly return a value. */
    "noFallthroughCasesInSwitch": true,                  /* Report errors for fallthrough cases in switch statements. */

    /* Completeness */
    "skipLibCheck": true                                 /* Skip type checking all .d.ts files (speeds up compilation). */
  },
  // These are crucial for telling TypeScript which files to compile
  "include": [
    "src/**/*.ts",                                       /* Include all TypeScript files (.ts) in your 'src' directory. */
    "src/**/*.tsx"                                       /* Include all TypeScript JSX files (.tsx) in your 'src' directory. */
  ],
  "exclude": [
    "node_modules",                                      /* Exclude all files in 'node_modules' from compilation. */
    "dist",                                              /* Exclude your compiled output directory. */
    "test-app",                                          /* Exclude your dummy 'test-app' files from compilation. */
    "**/*.test.ts"                                       /* Exclude test files from compilation. */
  ]
}